import 'package:flutter/material.dart';

class ChangeNotifiPrivider extends StatelessWidget {
  const ChangeNotifiPrivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(
      appBar: AppBar(
        title: Text("ChangeNotifiProvider"),
      ),
      body: Column(

      ),
    ));
  }
}
