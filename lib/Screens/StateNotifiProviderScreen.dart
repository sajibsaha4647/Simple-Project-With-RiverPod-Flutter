import 'package:flutter/material.dart';

class StateNififiProviderScreen extends StatelessWidget {
  const StateNififiProviderScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SafeArea(child: Scaffold(
      appBar: AppBar(
        title: Text("StateNotifiProvider"),
      ),
      body: Column(

      ),
    ));
  }
}
